{
  "ticket_id": "p2-t03",
  "phase_id": "p2",
  "title": "Simplify resume logic to continue the exact active transaction",
  "canonical_source": "docs/v0.6.0/plan.md",
  "source_plan_phase": "Implementation Strategy / Phase 2",
  "source_plan_sections": [
    "Canonical Invariants",
    "Canonical State Machine",
    "Resume Model",
    "Post-terminal materialization",
    "Implementation Strategy"
  ],
  "covered_requirements": ["REQ-P2-EXACT-RESUME"],
  "goal": "Implement the plan-defined rule that resume continues the exact active transaction from healthy materialized state using top-level next_action, with rebuild only when materialized state is missing, incomplete, or inconsistent.",
  "scope": [
    "Make healthy materialized state the primary resume anchor.",
    "Use rebuild only under the fallback conditions described in plan.md.",
    "Ensure resume reuses the exact active tx_id and does not replace it with heuristic candidates.",
    "Ensure valid top-level next_action is the primary continuation guide."
  ],
  "out_of_scope": [
    "Changing tx_id issuance semantics outside p2-t01.",
    "Broad compatibility redesign outside bounded behavior.",
    "Introducing extra continuation metadata not present in plan.md."
  ],
  "inputs": [
    "docs/v0.6.0/plan.md",
    "docs/v0.6.0/phase0-implementation-map.md",
    "docs/v0.6.0/ops-tool-contract-alignment-plan.md",
    "docs/v0.6.0/p1-t01.json",
    "docs/v0.6.0/p1-t02.json",
    "docs/v0.6.0/p1-t03.json"
  ],
  "expected_outputs": [
    "Runtime changes that continue the exact active transaction deterministically.",
    "Verification evidence showing resume prefers healthy materialized state and valid next_action.",
    "A code-to-plan traceability summary for exact resume behavior.",
    "Implementation changes that align ops_* helper contracts with exact-active-transaction continuation and next_action-first resume behavior."
  ],
  "acceptance_criteria": [
    "Resume never mints a new tx_id for an existing active transaction.",
    "Resume never replaces the active transaction with a heuristic candidate when exact active state is available.",
    "Valid canonical next_action is preferred over status-derived heuristics and reconstructed guesses.",
    "Post-terminal work is not resumed as active work.",
    "When ops_* helper contracts are updated in scope for exact-resume behavior, they reference docs/v0.6.0/ops-tool-contract-alignment-plan.md and do not introduce heuristic active-transaction replacement or status-over-next_action continuation."
  ],
  "dependencies": ["p1-t01", "p1-t02", "p1-t03", "p2-t01", "p2-t02"],
  "enables": ["p3-t01", "p3-t02"]
}
