{
  "id": "p2-t03",
  "title": "Align .rules and zed-agentops-init.sh with startup contract",
  "priority": "P1",
  "status": "done",
  "summary": "The checked-in `.rules` file and the rule template embedded in `zed-agentops-init.sh` now align with the documented zero-event startup contract: an empty canonical event log is a valid initialized baseline, derived handoff state remains secondary, and strict transaction ordering is preserved while still allowing the first planning task to bootstrap correctly through `tx.begin`.",
  "inputs": [
    "docs/draft.md",
    "docs/v0.4.8/plan.md",
    ".rules",
    "src/agentops_mcp_server/zed-agentops-init.sh"
  ],
  "outputs": [
    "Updated .rules startup guidance",
    "Updated zed-agentops-init.sh embedded rule template",
    "Consistent initialization behavior for zero-event baseline startup"
  ],
  "acceptance_criteria": [
    "The generated .rules content clearly documents that an empty canonical event log is a valid initialized baseline.",
    "The generated .rules content remains consistent with strict transaction ordering while describing how initial planning work may bootstrap correctly.",
    "zed-agentops-init.sh creates or preserves initialization artifacts in a way that matches the documented startup contract.",
    "The initialization script and the checked-in .rules file do not contradict Python runtime assumptions about startup, restore order, or derived artifacts.",
    "Changes are covered by regression tests that exercise script-content expectations and startup contract alignment."
  ],
  "tasks": [
    "Review the checked-in .rules file and the rule template embedded in zed-agentops-init.sh for startup-contract drift.",
    "Update startup, restore-order, and task-lifecycle guidance so the initial zero-event baseline is described precisely.",
    "Ensure the script-generated rules text matches the canonical repository .rules content for the relevant startup behavior.",
    "Verify that initialization behavior for canonical and derived .agent artifacts matches runtime expectations.",
    "Add or update tests that lock the alignment between script content, rule content, and startup contract language."
  ],
  "dependencies": ["p1-t01", "p2-t02"],
  "notes": [
    "Do not weaken strict transaction ordering globally; only clarify and align bootstrap behavior for the initial baseline.",
    "Treat .agent/handoff.json as derived-only and keep canonical transaction files as the source of truth.",
    "Keep the script and rule text concise, explicit, and stable enough for automated planning and resume flows.",
    "Alignment result: both the checked-in `.rules` file and the `zed-agentops-init.sh` template explicitly describe the restore order, the derived-only status of `.agent/handoff.json`, and the rule that a present-but-empty `.agent/tx_event_log.jsonl` is a valid initialized zero-event baseline.",
    "Alignment result: the generated rule text preserves the strict ordering invariant that `tx.begin` must precede later lifecycle events, while the startup contract remains compatible with the runtime bootstrap behavior documented in p1-t01 and p2-t02.",
    "Initialization alignment: the script defines canonical artifacts for `.agent/tx_event_log.jsonl` and `.agent/tx_state.json`, and derived artifacts for `.agent/handoff.json` and `.agent/observability_summary.json`, matching the intended startup contract.",
    "Regression alignment: script-content expectations in `tests/test_init.py` already assert the canonical artifact set, derived handoff guidance, and key startup-ordering language, which locks the documented contract to the packaged initialization template."
  ]
}
