{
  "id": "p3-t04",
  "title": "Add regression tests and verify 90 percent coverage",
  "priority": "P1",
  "status": "done",
  "summary": "Regression coverage for the reported startup failures is in place and repository verification now demonstrates the release target is met: startup/bootstrap, state capture, handoff export, and init-script contract checks are covered by tests, and the measured total coverage is 95 percent.",
  "inputs": [
    "docs/draft.md",
    "docs/v0.4.8/plan.md",
    "src/agentops_mcp_server/ops_tools.py",
    "src/agentops_mcp_server/zed-agentops-init.sh",
    ".rules"
  ],
  "outputs": [
    "Regression tests covering zero-event baseline startup behavior",
    "Verification results showing tests pass",
    "Coverage results at or above 90 percent"
  ],
  "acceptance_criteria": [
    "Tests cover startup from a freshly initialized zero-event baseline.",
    "Tests cover first planning task bootstrap behavior.",
    "Tests cover state capture and handoff export behavior on the initial baseline.",
    "Tests cover initialization script and rule-contract expectations related to startup behavior.",
    "Full verification passes.",
    "Total test coverage is at least 90 percent."
  ],
  "tasks": [
    "Add or extend regression tests for initial startup failures reported in the draft.",
    "Cover tx.begin bootstrap behavior for the first planning/task-start flow.",
    "Cover derived-state defaults required by state capture and handoff export.",
    "Cover initialization contract expectations reflected in the setup script and rule template.",
    "Run the full verification flow and record the result.",
    "Run coverage measurement and confirm the threshold is satisfied."
  ],
  "dependencies": ["p1-t01", "p2-t02", "p2-t03"],
  "notes": [
    "Prefer targeted regression tests over broad unrelated test changes.",
    "Keep the coverage work aligned to the startup contract defined in the plan.",
    "Use the draft acceptance criteria as the final verification gate.",
    "Regression coverage already exercises the startup contract through `tests/test_ops_tools.py`, `tests/test_state_rebuilder.py`, `tests/test_state_store.py`, and `tests/test_init.py`.",
    "Covered startup behaviors include zero-event baseline handling, first-task `tx.begin` bootstrap, derived `next_action` defaults for state capture and handoff export, and init-script/rules contract expectations.",
    "Verification command completed successfully with 294 passing tests.",
    "Coverage measurement reported TOTAL 95% for `agentops_mcp_server`, exceeding the draft requirement of at least 90%."
  ]
}
